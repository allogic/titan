FILE (GLOB_RECURSE SRC "${CMAKE_CURRENT_SOURCE_DIR}/src/*.c")

ADD_LIBRARY (spirv_reflect STATIC ${SRC})

TARGET_COMPILE_DEFINITIONS (spirv_reflect PUBLIC "_CRT_SECURE_NO_WARNINGS")

TARGET_INCLUDE_DIRECTORIES (spirv_reflect PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")

TARGET_LINK_LIBRARIES (spirv_reflect PUBLIC spirv)