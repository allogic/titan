FILE (GLOB_RECURSE SRC "${CMAKE_CURRENT_SOURCE_DIR}/src/*.c")

ADD_LIBRARY (fs STATIC ${SRC})

TARGET_COMPILE_DEFINITIONS (fs PUBLIC "_CRT_SECURE_NO_WARNINGS")

TARGET_INCLUDE_DIRECTORIES (fs PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
